"""Archived V1 modules. Do not import from production code paths.

Anything in this package is kept only because an external consumer (the
OpenClaw skills today) still depends on it. New code should target the
V2 replacements documented in each archived module's deprecation header.
"""
